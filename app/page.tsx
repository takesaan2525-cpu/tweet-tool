import { redirect } from 'next/navigation';

/* ───────────────────────────────────────────────
   このプロジェクト（zero-umeda）は「店舗に納品した道具」専用。

   ★2026-08-10：ここにあった営業ツール（DMテンプレ・営業リスト）は
     別プロジェクト growup-sales へ完全に移した。

     理由＝店側に渡しているURLは https://zero-umeda.vercel.app/dashboard で、
     末尾の /dashboard を消すだけでトップに来られる。そこに営業ツールが
     置いてあると、他店の名前・LINE ID・「成約/断られた」まで店側に見えてしまう。
     ★店舗が増えるほど危険が増す形だったので、同居をやめた。

   ★トップは店のダッシュボードへ送る。404にしないのは、店の人がURLから
     /dashboard を消してしまっても、何事もなく店の画面に戻るようにするため。
─────────────────────────────────────────────── */
export default function Page() {
  redirect('/dashboard');
}
